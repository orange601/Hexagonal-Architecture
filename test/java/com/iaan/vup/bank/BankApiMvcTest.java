package com.iaan.vup.bank;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

public class BankApiMvcTest {
	@DisplayName("Sample")
	void filedownloadTest() {
	/*
		// 1. given
		String pjtCd = "210001";
		Long fileSeqNo = 5375L;
		AttachFile attachFile = AttachFile.builder(	)
				.pjtCd(pjtCd)
				.fileSeqNo(fileSeqNo)
				.fileNm("A1-1L_FAB_EXX_XF-0_Central_1.bobj")
				.filePath("D:\\DTD_TF\\bobj\\20210902\\ASAN_A1_P1_1F_S3_H23\\A1-1L_FAB_EXX_XF-0_Central_1.bobj")
				.build();
		
		given(attachFileService.findByFileSeqNo(fileSeqNo)).willReturn(attachFile);
		
		// 2. when
		final ResultActions actions = mvc.perform(
				get("/v1/afile/download")
				.param("pjtCd", pjtCd)
				.param("fileSeqNo", String.valueOf(fileSeqNo))
				.contentType(MediaType.APPLICATION_OCTET_STREAM)
				)
				;
		
		// 3. then
		actions.andExpect(status().isOk()).andDo(print());
	*/
	}

}
