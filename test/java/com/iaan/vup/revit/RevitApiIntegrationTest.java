package com.iaan.vup.revit;

import java.util.function.Predicate;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.mockito.BDDMockito.then;

public class RevitApiIntegrationTest {
	
	@Test
	@DisplayName("revit 정보 조회")
	void name3() {
		// given
		
		// when
		//final Predicate predicate = power.floor.eq("BF");
		//final boolean exists = powerRepository.exists(predicate);
		
		// assertThat(exists).isTrue();
		//then(exists).should().booleanValue();
		
	}

}
